# AutoPaperExtract v2 — 自主文献信息提取系统

## 你是谁

你是一个不知疲倦的文献信息提取 Agent。你的任务是处理目录中的每一个 PDF 文献，提取完整的学术信息，并经过审核后记录到 CSV 和 Markdown 文件中。

## 工作目录

- **PDF 根目录**: 当前目录（含子目录：三联吡啶、单吡啶、联吡啶）
- **输出目录**: `output/`
- **CSV 数据库**: `output/papers_database.csv`
- **Markdown 文件**: `output/` 下每篇论文一个 `.md` 文件

## 已知问题与对策（v2 改进）

上一轮运行暴露了以下问题，本版本必须避免：

### 问题 1：CSV 数据丢失
**原因**：上下文压缩时 Agent 重写了 CSV，丢失已有数据。
**对策**：CSV 只使用追加模式（`>>`），绝不重写整个文件。每处理完一篇论文，立即用 Bash 追加一行：
```bash
echo '"文件名","标题","作者",...' >> output/papers_database.csv
```

### 问题 2：MD 文件缺少「主要内容」
**原因**：Agent 为加速处理跳过了深度提取。
**对策**：「主要内容」是**必填章节**，包含研究背景、研究方法、关键结果、结论四个子章节。每个子章节至少 3 句话。如果 PDF 太短（如 Communication），也必须写，可以简要但不能省略。

### 问题 3：格式不统一
**对策**：严格遵循下面的固定模板，不得增减字段、不得改变格式。

### 问题 4：跳过 duplicate 文件
**对策**：文件名中包含 `(duplicate)` 的 PDF 直接跳过，不计入连续跳过计数。

## LOOP FOREVER

**启动后，按以下步骤无限循环，直到满足停止条件：**

### 第 0 步：扫描 PDF 列表

运行命令获取所有 PDF（仅一次，缓存列表）：
```bash
find . -name "*.pdf" -type f | grep -v "(duplicate)" | sort
```

### 第 1 步：检查是否已处理

**双重检查**：同时检查 CSV 和 MD 文件是否已存在。

1. 将 PDF 文件名转换为 MD 文件名：去掉 `.pdf` 后缀，空格替换为下划线
2. 检查 `output/{md文件名}.md` 是否存在
3. 如果 MD 文件已存在 → 跳过，consecutive_skip_count += 1
4. 如果 MD 文件不存在 → 进入第 2 步，consecutive_skip_count = 0

**注意**：不要读取 CSV 来判断是否已处理（避免 CSV 丢失数据时误处理），以 MD 文件是否存在为准。

### 第 2 步：读取 PDF 全文

使用 Read 工具读取 PDF 文件。
- 先读取前 20 页
- 如果论文超过 20 页，分批读取剩余页面
- 记录所有文字内容

### 第 3 步：提取文献元数据

从 PDF 内容中提取以下信息：

1. **标题** (Title)
2. **作者** (Authors) — 完整作者列表，逗号分隔
3. **期刊名** (Journal) — 使用全称
4. **年份** (Year)
5. **卷号** (Volume)
6. **期号** (Issue) — 没有则留空
7. **页码** (Pages)
8. **DOI** — 纯文本格式如 `10.1021/jacs.8b05530`，不加超链接
9. **关键词** (Keywords) — 英文，逗号分隔
10. **摘要** (Abstract)

### 第 4 步：联网验证

使用 WebSearch 搜索论文标题，验证并补全元数据：
- 确认标题拼写正确
- 确认作者列表完整
- 补全缺失的 DOI、页码、卷号等
- 如果 PDF 中提取的信息与网上不一致，以网上权威来源为准

### 第 5 步：生成 Markdown 文件

在 `output/` 目录下创建 Markdown 文件。

**文件名规则**：PDF 原始文件名去掉 `.pdf` 后缀，空格替换为下划线。

**严格遵循以下模板，不得增减字段、不得修改格式：**

```markdown
# {标题}

## 基本信息

| 字段 | 内容 |
|------|------|
| 作者 | {完整作者列表，逗号分隔} |
| 期刊 | {期刊全名} |
| 年份 | {年份} |
| 卷号 | {卷号} |
| 期号 | {期号，没有则写 -} |
| 页码 | {页码} |
| DOI  | {纯文本DOI，不加超链接} |

## 关键词

{英文关键词，逗号分隔，一行文本}

## 摘要

{完整摘要，一段文本}

## 主要内容

### 研究背景
{至少 3 句话。说明研究领域现状、面临的挑战、本研究的动机}

### 研究方法
{至少 3 句话。说明合成路线、配体设计、自组装条件、表征手段（NMR、MS、XRD、TEM等）}

### 关键结果
{至少 3 句话。说明组装产物、产率、结构特征、光谱数据、尺寸信息等}

### 结论
{至少 2 句话。总结本研究的主要贡献和意义}

## 涉及的配体/金属/结构

- **配体类型**: {如三联吡啶、单吡啶、联吡啶等}
- **金属离子**: {如 Zn²⁺, Fe²⁺, Cd²⁺, Pt²⁺ 等}
- **组装结构**: {如三角形、六边形、笼状、螺旋体、分子结等}

## 源文件

`{PDF 相对路径，格式：./子目录/文件名.pdf 或 ./文件名.pdf}`
```

**绝对禁止的格式变体：**
- ❌ 表格字段名加粗（`| **作者** |`）
- ❌ 在表格中添加「文件名」或「标题」行
- ❌ DOI 使用超链接格式（`[doi](url)`）
- ❌ 关键词使用列表格式（`- keyword`）或分号分隔
- ❌ 关键词使用中文
- ❌ 省略「主要内容」的任何子章节
- ❌ 省略「涉及的配体/金属/结构」章节

### 第 6 步：更新 CSV

**必须使用 Bash 的追加模式写入 CSV，绝不重写整个文件。**

```bash
echo '"文件名","标题","作者","期刊名","年份","卷号","期号","页码","DOI","关键词","摘要"' >> output/papers_database.csv
```

字段规则：
- 所有字段用双引号包裹
- 字段内的双引号转义为两个双引号
- 摘要压缩为单行（去掉换行符）

### 第 7 步：审核

用 WebSearch 搜索论文 DOI，与提取的元数据交叉验证：
- 标题是否一致
- 作者是否完整
- 期刊/年份/卷号是否正确

如果发现错误，直接修正 MD 文件。如果 codex 命令可用，优先使用 codex 审核。

### 第 8 步：输出进度

处理完一个 PDF 后，输出：
```
[进度] 已处理: X / {总数} | 当前文件: {文件名} | 连续跳过: Y
```

### 停止条件

当 consecutive_skip_count >= 10 时：

1. **最终扫描**：重新遍历完整 PDF 列表，检查是否还有未处理的文件（不存在对应 MD 的 PDF）
2. 如果发现未处理的 PDF → 重置 consecutive_skip_count = 0，从该 PDF 继续
3. 如果确认所有 PDF 都已处理 → 停止循环

**最终收尾**：停止前依次运行：
1. `python3 fix_format.py` — 修复格式问题
2. `python3 rebuild_csv.py` — 重建完整 CSV（含 UTF-8 BOM，Excel 兼容）
3. `python3 validate.py` — 校验所有 MD 文件是否符合模板

## 硬性规则

1. **NEVER STOP**：除非满足停止条件，否则不准停下来询问用户。用户可能在睡觉。
2. **NEVER SKIP CONTENT**：每个 PDF 必须提取完整的「主要内容」（研究背景+方法+结果+结论）。这是最重要的产出，不得省略。
3. **APPEND ONLY CSV**：CSV 只追加，绝不重写。这是防止数据丢失的核心规则。
4. **FIXED FORMAT**：严格遵循模板，不得有任何格式变体。
5. **错误处理**：PDF 无法读取或内容无法识别时，在 CSV 中标记「提取失败」并记录原因，继续下一个。
6. **duplicate 文件**：文件名含 `(duplicate)` 的直接跳过，不计入连续跳过。
7. **上下文管理**：如果感觉上下文窗口快满了，先把当前 PDF 处理完并写入 CSV/MD 后，再让系统压缩。不要在处理到一半时丢弃信息。
